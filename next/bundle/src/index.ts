/**
 * The yzj-next bundle package: a patch layer (cordis.patch.yml) plus this
 * entry so the package remains a valid ESM module with a declaration. All
 * behavior lives in the mounted rows.
 */
export const name = 'yzj-next-bundle'
