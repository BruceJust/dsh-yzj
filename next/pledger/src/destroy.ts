/**
 * 销毁口令 —— **一个字面，一处定义**.
 *
 * 它住在自己的文件里，而不是在 `desk.ts` 或 `vault.ts` 任一处，因为那两个模块彼此
 * 依赖（面读投影），而这句话两边都要用。放在中间，就没有人需要抄第二份。
 *
 * 为什么销毁要**打字**而不是点两下：数据主权归人，组织扣押成长账本就是 Org–P–Org′
 * ——所以这扇门必须真的存在。可它同时是这本账**唯一**的删除路径（更正一律走追加），
 * 所以推开它得有一次真的动作。一次打字和一次点击的差别，正好是「我确实想」和
 * 「我手滑了」的差别。
 */
export const DESTROY_PHRASE = '销毁我的金库'
