/**
 * The dsh-yzj bundle package: a patch layer only (cordis.patch.yml), plus
 * this node entry so the package remains a valid ESM module with a type
 * declaration. All behavior lives in the mounted rows.
 */
export const name = 'dsh-yzj-bundle'
